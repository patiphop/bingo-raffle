{
  "name": "bingo-raffle-webapp",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Bingo (5x5) and Raffle web app — static frontend on GitHub Pages + Google Apps Script backend",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host --port 5174",
    "test": "vitest --watch=false",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "jsdom": "^26.1.0",
    "vite": "^5.4.0",
    "vitest": "^3.2.4"
  }
}
